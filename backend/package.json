{
  "name": "ablitmd-backend",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "dotenvx run --env-file .env.backend -- node --watch bin/www",
    "start": "dotenvx run --env-file .env.backend -- node bin/www",
    "test": "vitest run",
    "test:watch": "vitest",
    "seed": "node db/seed.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-async-handler": "^1.2.0",
    "mongoose": "^9.6.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^25.7.0",
    "mongodb-memory-server": "11.1.0",
    "supertest": "^7.0.0",
    "vitest": "4.1.6"
  }
}
