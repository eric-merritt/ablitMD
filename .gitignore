# Run data — keep .gitkeep, ignore actual run files
data/runs/*.json
data/runs/*/
data/runs/*.bak

# Local data — backups, mirrors, generated artifacts, not for git
data/charts/
data/mongo_backup/
data/vastai_mirror/
root@154.59.156.8

# Python
__pycache__/
*.pyc
.venv/

# Secrets
.env
backend/.env
frontend/.env

# Node
node_modules/

# Local-only — Claude config, not for git
CLAUDE.md
.claude/
docs/superpowers/
